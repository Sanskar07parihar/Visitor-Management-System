@model IEnumerable<Visitor_Management_System.Models.Ride>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Park)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.CustomerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Park.ParkId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RideId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RideId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RideId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
